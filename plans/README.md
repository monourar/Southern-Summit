# Implementation Plans

Generated by the improve skill on 2026-08-04. These plans were written against
commit `555faf5` while the working tree contained uncommitted polish changes.
Execute in the order below unless dependencies say otherwise. Each executor
must read its plan fully, run its drift check, honor STOP conditions, and
update its status row after completion.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Establish verification net: typecheck, lint, tests, CI | P1 | M | — | DONE (verified in worktree; 0 errors/99 warnings, 2 tests pass, build green) |
| 002 | Delete dead legacy files and raw image originals | P2 | S | — | DONE (git rm staged; typecheck/tests/build green) |
| 003 | Bump dev-only build dependencies | P3 | S | — | DONE (postcss HIGH cleared via ^8.5.25; esbuild/vite chain remains — requires vite 5→8 major migration, out of scope per STOP) |
| 004 | Refactor estimator fee math into a data table | P2 | S | 001 | DONE (computeEstimate pure fn + 4 parametric tests; 6 tests pass) |
| 005 | Add honest fallback for failed mailto handoff | P2 | S | 001 | DONE (window.open handoff + blur heuristic; 3 modal tests pass; typecheck/lint/build green) |
| 006 | Gate custom-cursor rAF on visibility and reduced motion | P3 | S | 001 recommended | DONE (visibilitychange + media-query change gating; 4 cursor tests pass; typecheck/lint/build green) |
| 007 | Add AGENTS.md and README.md | P2 | S | 001 | DONE (README + AGENTS authored; DESIGN/PRODUCT staged; build green) |
| 008 | Wire and document the asset build pipeline | P3 | S | 002, 007 | DONE (optimize:images + fetch:fonts npm targets; README asset-pipeline section; fetch:fonts exits 0; build/typecheck/lint/tests green) |
| 009 | Design authenticated proposal intake | P2 | M | 007; informs 010 | DONE (docs/spikes/proposal-intake.md: two-option matrix, neutral domain/status/security contract, staged rollout, owner decisions; mailto fallback preserved; typecheck/build green; docs-only) |
| 010 | Design protected builder specification portal | P3 | M | 009 | DONE (docs/spikes/builder-portal.md: MVP/deferred scope, role permissions, package lifecycle + server-side authz, two boundaries, reversible rollout; reuses 009 identity/storage/retention; typecheck/build green; docs-only) |
| 011 | Test interactive patterns previously untested: comparison-slider keyboard control, portfolio filter→carousel sync, modal Tab trap | P2 | M | 001 (landed) | TODO |
| 012 | Reconcile docs bookkeeping so the improve index stays authoritative: document the vite/vitest-audit defer path and the index.html live-marker hygiene caveat in the plans ledger, so future runs stop flagging already-decided territory as open risk | P3 | S | none | TODO |
| 013 | Investigate & design the proposal-intake post-submission experience: what the visitor sees after the mailto fallback resolves, and the smallest durable interaction contract before a backend exists (direction finding; lands the moment a maintainer says the modal needs more than "Request Prepared") | P2 | S–M | 005 (landed), 009 (landed: intake model exists in docs/spikes/proposal-intake.md) | TODO |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED (reason)` |
`REJECTED (rationale)`.

## Dependency notes

- **001 is currently blocked pending a fresh execution attempt.** The first
  executor stop found that `src/test-setup.ts` was missing from the scope; the
  plan was corrected. Two revision attempts then found peer conflicts between
  the originally specified ESLint 9 range and `typescript-eslint`/`eslint-plugin-react-hooks`.
  Plan 001 is now refreshed to the compatible ESLint 8.57 + TypeScript ESLint
  7 range. Start a new execution attempt rather than bypassing peer checks.

- **001 first for test-dependent work.** Plans 004 and 005 require the Vitest
  and React Testing Library harness created by 001. Plan 006 can proceed
  without it, but its tests are stronger if 001 has landed.
- **002 is independent and safe early.** It removes the unreferenced
  pre-React `script.js`, the tracked orphan `styles.css`, and raw source image
  originals. Plan 008 must follow 002 because its README explains that raw
  images are local, untracked inputs to the optimizer.
- **003 is independent.** It only changes dev-tool dependency versions; run it
  before or after 001.
- **007 should follow 001.** The README needs to document the final test/lint
  commands. It also tracks the already-authored `DESIGN.md` and `PRODUCT.md`
  without changing their content.
- **008 follows 002 and 007.** It adds npm targets and the asset-pipeline
  section to the onboarding docs.
- **009 is a design spike, not a backend implementation.** It defines the
  shared identity, intake, storage, retention, and security boundary.
- **010 depends on 009.** The builder portal must reuse the intake spike's
  identity/project/document decisions instead of creating a second system.

## Findings covered

- F1 — no verification/regression net: plan 001.
- F2 — dead legacy files and raw source bloat: plan 002.
- F6 — dev-only esbuild/postcss/vite advisories: plan 003.
- F4 — estimator arithmetic embedded in render: plan 004.
- F3 — mailto handoff has no failure path: plan 005.
- F7 — custom cursor rAF is not visibility/media-change gated: plan 006.
- F5 — missing README/AGENTS onboarding: plan 007.
- D3 — undocumented asset pipeline: plan 008.
- D1 — authenticated proposal intake direction: plan 009.
- D2 — protected builder portal direction: plan 010.

## Audit round 2 (2026-08-05, commit a2779fd)

A second advisor pass ran after plans 001–010 landed. Vetted, in-code-confirmed
findings and their dispositions:

- **adopted as plan 011** — no tests cover the image-comparison slider's keyboard
  contract (Home/End/Arrow), the portfolio filter→carousel scroll-sync, or the
  consultation modal's Tab trap; all three are user-facing a11y-critical paths.
- **adopted as plan 012** — docs bookkeeping: the vite/vitest audit chain
  (esbuild moderate, vite high, vitest critical) was intentionally deferred by
  003's STOP on the major migration, and the impeccable-live marker pair sits in
  `index.html` (the `.impeccable/live` tooling injects it at `_self`-scoped
  start/end markers during live sessions). Both facts were open risk surface to
  a fresh audit; 012 records the decisions so they stop re-surfacing.
- **adopted as plan 013** — direction finding: the proposal-intake flow is
  designed (009) but the post-submit visitor experience is static copy plus the
  005 mailto-failure fallback; the next durable contract is worth a designed
  decision now, before any backend lands.
- **rejected — perf-duplication**: "CI test speed" overlapped 001's work
  (workflow already has `cache: npm` at `.github/workflows/ci.yml:19`); the
  surviving local 26 s wall-clock is jsdom boot per file, with no executor-safe
  knob that lowers it materially without splitting the harness. Not worth doing.
- **rejected — redundancy**: "portfolio srcset missing widths" and "footer
  contact facts unverified" were re-checked in code/images: the grid images
  exist on disk at both widths, and contacts are owner-confirmed real. No flaw.

## Deferred risk ledger

Pinned decisions a fresh advisor pass must not re-flag as open risk:

- **vite/vitest audit chain (esbuild moderate; vite high; vitest critical)**:
  deliberate defer per 003's STOP on a Vite 5→8 + Vitest 1→4 major migration.
  Locked scope: only `package.json` devDependencies for the build chain; no
  runtime surface. Re-open only as a verified major-bump plan with a
  test-matrix story, not as background noise.
- **`index.html:33-35` impeccable-live marker pair** (`localhost:8400/live.js`):
  injected by the `.impeccable/live` tooling during live sessions; not a source
  defect to patch by editing `index.html`. The hook owns insert/removal between
  the start/end markers per session. Do not flag as runtime or a11y risk unless
  a live-tooling contract change decides otherwise. See `.impeccable/live/config.json`.

## Findings considered and rejected

- `index.html:33-35` live-server markup: rejected as a production bug because
  it is dev-tool injection and does not affect the built runtime. Keep it out
  of product code; remove it only if the live tooling documents a cleanup path.
- Duplicate raw-image cleanup as a separate plan: folded into F2 because the
  dead `script.js`, orphan `styles.css`, and superseded raw images are one
  repository-hygiene cleanup with the same low-risk verification story.
- A separate plan for `@tailwindcss/postcss`: rejected for now. The active
  `postcss.config.js` uses the Tailwind 3 plugin and `src/styles/index.css`
  uses Tailwind 3 directives; plan 003 explicitly forbids an opportunistic
  Tailwind v3→v4 migration.
- Broad `CustomCursor` rewrite: rejected. Plan 006 only gates the existing rAF
  and listeners; visual or interaction redesign is not justified by the audit.

## Audit limits

The audit covered the tracked React/Vite source, build scripts, package
manifest/lockfile posture, product/design intent docs, and repository hygiene.
It did not perform a browser-based visual regression run, production-host
inspection, real mail-client testing, provider/privacy due diligence, or a
deep review of binary image/font provenance. Those are called out as explicit
verification or open-decision work in the relevant plans.
